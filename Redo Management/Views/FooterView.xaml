<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Redo_Management.Views.FooterView">
    <!-- Footer Wrapped in Border -->
    <Border Grid.Row="2" StrokeThickness="2" Padding="10" BackgroundColor="White">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="10" />
        </Border.StrokeShape>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" 
               HorizontalTextAlignment="Start" Text="Terms of Use" />
            <Label Grid.Row="0" Grid.Column="2" 
               HorizontalTextAlignment="End" Text="Privacy Policy" />
            <Image Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" 
               Source="redo_arrow_ai.png" HeightRequest="60" />
        </Grid>
    </Border>
</ContentView>
