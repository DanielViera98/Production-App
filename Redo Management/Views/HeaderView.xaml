<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Redo_Management.Views.HeaderView">
    <!-- Header Grid -->
    <Grid Grid.Row="0" BackgroundColor="Black" VerticalOptions="Start">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"
                Text="Redo™" TextColor="#73094F" Style="{StaticResource SubHeadline}"/>
        <Label Grid.Row="0" Grid.Column="1" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"
                Text="Position" TextColor="#73094F" Style="{StaticResource SubHeadline}"/>
        <!-- TODO: Add position code -->
        <Label Grid.Row="0" Grid.Column="2" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"
                Text="Name" TextColor="#73094F" Style="{StaticResource SubHeadline}"/>
        <!-- TODO: Add name code-->
    </Grid>
</ContentView>
